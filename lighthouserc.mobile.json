{
  "//": [
    "MOBILE. The desktop config is the sibling file; this one exists because",
    "CLAUDE.md says a working mobile experience is not optional, and nothing was",
    "measuring it. There is no preset here on purpose — mobile is what Lighthouse",
    "does by default: 412x823, simulated slow 4G, and a 4x CPU slowdown on top of",
    "everything that already costs this site its desktop score.",
    "",
    "The performance floor is NOT set yet, deliberately. Setting it before the",
    "first measurement means either inventing a number or copying the desktop",
    "one, and a threshold picked so that it passes is the habit this repository",
    "spent a week unlearning. This run asserts only what should not depend on the",
    "device — accessibility, SEO, layout shift — and archives the report. The",
    "floor goes in from the number the first run actually produces."
  ],
  "ci": {
    "collect": {
      "staticDistDir": "./dist",
      "url": [
        "http://localhost/index.html"
      ],
      "numberOfRuns": 3,
      "settings": {
        "skipAudits": [
          "uses-http2",
          "canonical",
          "is-crawlable"
        ]
      }
    },
    "assert": {
      "assertions": {
        "categories:accessibility": [
          "error",
          {
            "minScore": 0.9
          }
        ],
        "categories:seo": [
          "error",
          {
            "minScore": 0.9
          }
        ],
        "cumulative-layout-shift": [
          "error",
          {
            "maxNumericValue": 0.1
          }
        ],
        "total-byte-weight": "off",
        "unused-javascript": "off",
        "unminified-javascript": "off",
        "unminified-css": "off",
        "unused-css-rules": "off",
        "legacy-javascript": "off",
        "uses-long-cache-ttl": "off",
        "csp-xss": "off",
        "third-party-cookies": "off",
        "render-blocking-resources": "off",
        "non-composited-animations": "off"
      }
    }
  },
  "//upload": [
    "There is deliberately no `upload` block. There was one, pointing at",
    "./.lighthouseci-mobile, and it never did anything: the GitHub action",
    "hardcodes its results directory to $PWD/.lighthouseci and runs its own",
    "`lhci upload --target=filesystem --outputDir=...` without passing this",
    "config at all. So the setting looked like configuration and was inert,",
    "and the CI step that archived ./.lighthouseci-mobile archived nothing",
    "while reporting a warning nobody read. Uploading is the action's job;",
    "see .github/workflows/ci.yml."
  ]
}
